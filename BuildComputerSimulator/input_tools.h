#pragma once
#include <iostream>
#include <string>
#include "menu.h"

using namespace std;

string inputString(bool = false, string = "");
double inputNumber(double = -DBL_MAX, double = DBL_MAX, string = "");